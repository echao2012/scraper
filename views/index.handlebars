<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Mongo Scraper</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/saved">Saved Articles</a>
            </li>
        </ul>
        <form class="form-inline">
            <button type="button" class="btn btn-primary my-2 mr-2 scrape">Scrape New Articles</button>
            <button type="button" class="btn btn-primary my-2 mr-2 clear-all-articles">Clear All Articles</button>
        </form>
    </div>
</nav>

<div class="d-flex flex-row flex-wrap justify-content-center">
    {{#each articles}}
        <div class="card m-3" data-_id={{this._id}}>
            <img src={{this.imgUrl}} class="card-img-top">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">{{this.title}}</h5>
                <p class="card-text">{{this.subtitle}}</p>
                <button type="button" class="btn btn-primary mt-auto save-article">Save Article</button>
            </div>
        </div>
    {{/each}}
</div>

<script type="text/javascript" src="/assets/js/index.js"></script>